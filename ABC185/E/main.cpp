#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int N, M;
    vector<int> A(M), B(M);
    cin >> N >> M;
    for (int i = 0; i < M; i++){
        cin >> A.at(i);
    }
    for (int i = 0; i < M; i++){
        cin >> B.at(i);
    }
    
}
